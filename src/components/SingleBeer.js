//click beer --> display image, mname, tagline, first_brewed, attenuation_level, description, contributed_by
//console.log API response

import React, { Component } from 'react'

export default class SingleBeer extends Component {
    render() {
        return (
            <div>
                Single Beer
            </div>
        )
    }
}
